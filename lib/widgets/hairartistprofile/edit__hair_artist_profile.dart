import 'package:flutter/material.dart';

class EditHairArtist extends StatelessWidget {
  const EditHairArtist({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
